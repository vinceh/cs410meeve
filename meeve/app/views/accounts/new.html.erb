<%
@page_title 	= 'Register'
@page_heading 	= 'Register'
%>
<% form_for @account, :url => {:action => :new} do |f| %>
		
	<%= render :partial => "form_account_info", :locals => {:form => f}%>
	
	<p><%= f.submit 'Create' %></p>
<% end %>
<%= link_to 'Back',  :action => :index %>