<% form_for @event, :url => { :acion => :new} do |f| %>

	<%= render "shared/error_messages", :target => @event %>
	<%= render "form_event_info", :form => f %>
	<%= link_to "Back", :action => :index %>
	
<%= f.submit 'Create' %>
<% end %>