var id = "#join_button" + <%= @event %>;

$(id).children().remove();
$(id).append("<%= escape_javascript (button_to 'Quit', {:controller => :events, :action => :quit_event, :eid => @event}, :class => "gray_submit", :remote => true) %>");