<% form_for @event, :url => { :action => :remove, :eid => @event.event_id } do |f| %>
Are you sure you want to remove this event from the pool?
<%= f.submit 'Yes', :action => :destroy, :method => :delete %>
<%= link_to "No", :action => :index %>
<% end %>